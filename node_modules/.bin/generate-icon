#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@expo/vector-icons/node_modules/react-native-vector-icons/generate-icon.js" "$@"
  ret=$?
else 
  node  "$basedir/../@expo/vector-icons/node_modules/react-native-vector-icons/generate-icon.js" "$@"
  ret=$?
fi
exit $ret
